// Fichier de configuration (pour l'instant, basique - peut être étendu pour gérer différents environnements, etc.)
module.exports = {
    // Vous pouvez ajouter ici des variables de configuration, comme des ports, des clés API (si vous ne les mettez pas en variables d'environnement), etc.
    // Exemple (non utilisé actuellement, mais pour montrer le principe) :
    // serverPort: process.env.SERVER_PORT || 3000,
};